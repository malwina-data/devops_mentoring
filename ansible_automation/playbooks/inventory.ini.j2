{% for group, hosts in groups.items() %}
[{{ group }}]
{% for host in hosts %}
{{ host.name }} ansible_user={{ host.ansible_user }} ansible_ssh_private_key_file={{ host.ansible_ssh_private_key_file }} ansible_ssh_common_args='{{ host.ansible_ssh_common_args }}'
{% endfor %}
{% endfor %}
